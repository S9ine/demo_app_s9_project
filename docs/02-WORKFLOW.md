# 🔄 System Workflow
## Premium Security ERP System

**Last Updated:** December 12, 2025

---

## 📊 System Architecture

```
┌─────────────────────────────────────────────────────────────┐
│                         FRONTEND                            │
│                  (React + Vite + TailwindCSS)               │
│                    http://localhost:5173                    │
└───────────────────────────┬─────────────────────────────────┘
                            │ HTTP/REST API
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                         BACKEND                             │
│                (Python + FastAPI + SQLAlchemy)              │
│                    http://localhost:8000                    │
└───────────────────────────┬─────────────────────────────────┘
                            │ SQL
                            ▼
┌─────────────────────────────────────────────────────────────┐
│                        DATABASE                             │
│                   (PostgreSQL 15+)                          │
│                  localhost:5432/erp_db                      │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔐 Authentication Flow

```
┌──────────┐     POST /api/auth/login      ┌──────────┐
│  User    │ ────────────────────────────► │  Backend │
│ (Login)  │     {username, password}      │          │
└──────────┘                               └────┬─────┘
                                                │
                                                ▼
                                        ┌───────────────┐
                                        │ Verify Hash   │
                                        │ (Argon2)      │
                                        └───────┬───────┘
                                                │
                    JWT Token + User Info       ▼
┌──────────┐     ◄──────────────────────  ┌──────────┐
│  User    │     {token, user, permissions}│  Backend │
│ (Store)  │                               │          │
└──────────┘                               └──────────┘
      │
      │ Store in localStorage
      ▼
┌──────────────────────────────────────┐
│ All API calls include:               │
│ Header: Authorization: Bearer <token>│
└──────────────────────────────────────┘
```

---

## 📝 CRUD Operations Flow

### Create Flow (e.g., New Guard)

```
┌─────────┐    Click "เพิ่ม"    ┌─────────────┐
│  User   │ ─────────────────► │ Open Modal  │
└─────────┘                    └──────┬──────┘
                                      │
                               Fill Form Data
                                      │
                                      ▼
                               ┌─────────────┐
                               │  Click Save │
                               └──────┬──────┘
                                      │
                        POST /api/guards (no guardId)
                                      │
                                      ▼
┌────────────────────────────────────────────────────┐
│ Backend:                                           │
│ 1. Generate Auto ID (PG-XXXX)                     │
│ 2. Validate Data                                   │
│ 3. Save to Database                                │
│ 4. Create Audit Log (action: CREATE)              │
│ 5. Return new guard with ID                        │
└────────────────────────────────────────────────────┘
                                      │
                              Response with ID
                                      │
                                      ▼
                              ┌─────────────┐
                              │ Close Modal │
                              │ Refresh List│
                              └─────────────┘
```

### Update Flow

```
┌─────────┐    Click Edit     ┌─────────────┐
│  User   │ ─────────────────► │ Open Modal  │
└─────────┘                    │ (with data) │
                               └──────┬──────┘
                                      │
                               Edit Form Data
                               (ID is read-only)
                                      │
                                      ▼
                        PUT /api/guards/{id}
                                      │
                                      ▼
┌────────────────────────────────────────────────────┐
│ Backend:                                           │
│ 1. Find existing record                            │
│ 2. Compare old vs new (detect changes)             │
│ 3. Update Database                                 │
│ 4. Create Audit Log (action: UPDATE, changes: [...])│
│ 5. Return updated record                           │
└────────────────────────────────────────────────────┘
```

### Delete Flow

```
┌─────────┐   Click Delete    ┌──────────────────┐
│  User   │ ─────────────────► │ Confirm Dialog  │
└─────────┘                    └────────┬─────────┘
                                        │
                               Confirm "ยืนยันลบ"
                                        │
                         DELETE /api/guards/{id}
                                        │
                                        ▼
┌────────────────────────────────────────────────────┐
│ Backend:                                           │
│ 1. Find existing record                            │
│ 2. Create Audit Log (action: DELETE, oldData: {...})│
│ 3. Delete from Database                            │
│ 4. Return success                                  │
└────────────────────────────────────────────────────┘
```

---

## 📥 Excel Import Flow

```
┌─────────┐  Click "Import"   ┌─────────────────┐
│  User   │ ─────────────────► │ Import Modal   │
└─────────┘                    └────────┬────────┘
                                        │
                               Click "Download Template"
                                        │
                         GET /api/guards/template
                                        │
                                        ▼
                              ┌─────────────────┐
                              │ Open Excel      │
                              │ Fill Data       │
                              │ Save File       │
                              └────────┬────────┘
                                       │
                               Upload Excel File
                                       │
                         POST /api/guards/import
                                       │
                                       ▼
┌────────────────────────────────────────────────────┐
│ Backend:                                           │
│ 1. Parse Excel file (pandas)                       │
│ 2. Validate columns (expected 7 columns)           │
│ 3. For each row:                                   │
│    a. Generate Auto ID                             │
│    b. Validate data                                │
│    c. Save to Database                             │
│    d. Create Audit Log                             │
│ 4. Return: {success: X, failed: Y, errors: [...]} │
└────────────────────────────────────────────────────┘
                                       │
                              Show Result Summary
                                       │
                                       ▼
                              ┌─────────────────┐
                              │ Close Modal     │
                              │ Refresh List    │
                              └─────────────────┘
```

---

## 📤 Excel Export Flow

```
┌─────────┐  Select Rows (optional)  ┌─────────────────┐
│  User   │ ─────────────────────────► │ Click Export  │
└─────────┘                            └───────┬───────┘
                                               │
                                               ▼
┌────────────────────────────────────────────────────┐
│ Frontend:                                          │
│ 1. Get selected IDs (or all if none selected)     │
│ 2. Convert data to Excel format (xlsx)            │
│ 3. Create Blob and download                        │
│ 4. Call audit log API (action: EXPORT)            │
└────────────────────────────────────────────────────┘
                                               │
                                    Download Excel File
```

---

## 🔍 Audit Trail System

### What Gets Logged

| Action | Trigger | Data Logged |
|--------|---------|-------------|
| CREATE | New record created | `newData: {...}` |
| UPDATE | Record modified | `oldData, newData, changes[]` |
| DELETE | Record removed | `oldData: {...}` |
| IMPORT | Excel import | `newData` for each row |
| EXPORT | Excel export | `count, ids[]` |

### Audit Log Entry Example

```json
{
  "id": 123,
  "entityType": "guards",
  "entityId": 45,
  "action": "UPDATE",
  "changes": {
    "oldData": { "phone": "081-111-1111" },
    "newData": { "phone": "082-222-2222" },
    "changedFields": ["phone"]
  },
  "userId": 1,
  "username": "admin",
  "timestamp": "2025-12-12T10:30:00Z"
}
```

### View History Flow

```
┌─────────┐  Click History Icon  ┌──────────────────┐
│  User   │ ─────────────────────► │ History Modal   │
└─────────┘                        └────────┬────────┘
                                            │
                     GET /api/audit-logs?entityType=guards&entityId=45
                                            │
                                            ▼
                                   ┌─────────────────┐
                                   │ Timeline View   │
                                   │ - Who changed   │
                                   │ - What changed  │
                                   │ - When changed  │
                                   └─────────────────┘
```

---

## 🔄 Site with Shifts Flow

### Assign Shifts to Site

```
┌─────────┐  Edit Site         ┌─────────────────────────┐
│  User   │ ─────────────────► │ Site Form Modal         │
└─────────┘                    │ Section: ข้อมูลกะงาน     │
                               └───────────┬─────────────┘
                                           │
                            Select shifts from dropdown
                            Set number of workers per shift
                                           │
                                           ▼
                               ┌─────────────────────────┐
                               │ shiftAssignments JSON:  │
                               │ [                       │
                               │   {shiftId: 1, count: 2}│
                               │   {shiftId: 2, count: 3}│
                               │ ]                       │
                               └───────────┬─────────────┘
                                           │
                            PUT /api/sites/{id}
                                           │
                                           ▼
                               Save to Database
```

---

## 📊 Dashboard Data Flow

```
┌─────────┐  Open Dashboard    ┌──────────────────┐
│  User   │ ─────────────────► │ Dashboard Page   │
└─────────┘                    └────────┬─────────┘
                                        │
                    Parallel API Calls:
                    ├── GET /api/customers/stats
                    ├── GET /api/sites/stats
                    ├── GET /api/guards/stats
                    ├── GET /api/staff/stats
                    └── GET /api/daily-advances/stats
                                        │
                                        ▼
                               ┌─────────────────┐
                               │ Display Cards:  │
                               │ - Total counts  │
                               │ - Active/Inactive│
                               │ - Recent items  │
                               │ - Charts        │
                               └─────────────────┘
```

---

## ⚙️ Permission System

### Role-Based Access

```
┌─────────────────────────────────────────────────────┐
│ Admin Role                                          │
│ permissions: ["*"] or [all menus]                   │
│ → Can access everything                             │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ Manager Role                                        │
│ permissions: ["dashboard", "customers", "sites",    │
│               "guards", "staff", "daily-advances"]  │
│ → Cannot access settings, audit-logs                │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ User Role                                           │
│ permissions: ["dashboard", "daily-advances"]        │
│ → Limited access only                               │
└─────────────────────────────────────────────────────┘
```

### Menu Visibility Flow

```
┌─────────┐    Login Success    ┌────────────────────┐
│  User   │ ─────────────────► │ Get permissions    │
└─────────┘                     │ from response      │
                                └─────────┬──────────┘
                                          │
                                          ▼
                               ┌─────────────────────┐
                               │ Sidebar Component:  │
                               │ - Filter menu items │
                               │ - Show only allowed │
                               └─────────────────────┘
```

---

## 🚨 Error Handling Flow

```
┌─────────┐    API Request     ┌──────────────────┐
│ Frontend│ ─────────────────► │     Backend      │
└─────────┘                    └────────┬─────────┘
                                        │
                            Error occurs (validation, DB, etc.)
                                        │
                                        ▼
                               ┌─────────────────────┐
                               │ HTTPException:      │
                               │ {                   │
                               │   status: 400/401/500│
                               │   detail: "message" │
                               │ }                   │
                               └─────────┬───────────┘
                                         │
                         ◄───────────────┘
                                         │
                                         ▼
                               ┌─────────────────────┐
                               │ Frontend:           │
                               │ - Show toast/alert  │
                               │ - Log to console    │
                               │ - Keep form data    │
                               └─────────────────────┘
```
