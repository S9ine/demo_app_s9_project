# สรุปฟีเจอร์ Download Template และ Import Excel

## ✅ สิ่งที่ได้ทำเสร็จแล้ว

### 1. **อัปเดต Customer Template** 
สร้างไฟล์ Excel template ใหม่ที่ตรงกับโครงสร้างข้อมูลลูกค้าที่ปรับปรุงแล้ว

**ไฟล์:** `backend_python/templates/customer_template.xlsx`

**คอลัมน์ทั้งหมด 14 คอลัมน์:**
1. รหัสลูกค้า (Code) - **required**
2. ประเภทธุรกิจ (Business Type) - **ใหม่!** (กิจการเจ้าของคนเดียว, ห้างหุ้นส่วน, บริษัทจำกัด, รัฐวิสาหกิจ)
3. ชื่อลูกค้า (Name) - **required**
4. เลขประจำตัวผู้เสียภาษี (Tax ID)
5. ที่อยู่ (Address) - บ้านเลขที่, หมู่, ซอย, ถนน
6. แขวง/ตำบล (Sub-District) - **ใหม่!**
7. เขต/อำเภอ (District) - **ใหม่!**
8. จังหวัด (Province) - **ใหม่!**
9. รหัสไปรษณีย์ (Postal Code) - **ใหม่!**
10. ชื่อผู้ติดต่อหลัก (Contact Person)
11. เบอร์โทร (Phone)
12. อีเมล (Email)
13. ผู้ติดต่อรอง (Secondary Contact) - **ใหม่!**
14. เงื่อนไขการชำระเงิน (Payment Terms) - **ใหม่!**

**ไฟล์สำหรับสร้าง template:** `backend_python/update_template.py`

---

### 2. **Download Template Endpoint**
เพิ่ม API endpoint สำหรับดาวน์โหลด template

**Endpoint:** `GET /api/master-data/customers/template`

**ไฟล์:** `backend_python/app/api/master_data.py` (บรรทัด 37-50)

```python
@router.get("/customers/template")
async def download_customer_template(
    current_user: User = Depends(get_current_active_user)
):
    """Download Excel template for customer import"""
    template_path = "templates/customer_template.xlsx"
    
    if not os.path.exists(template_path):
        raise HTTPException(status_code=404, detail="Template file not found")
    
    return FileResponse(
        path=template_path,
        filename="customer_template.xlsx",
        media_type="application/vnd.openxmlformats-officedocument.spreadsheetml.sheet"
    )
```

**Features:**
- ✅ ต้อง Login ก่อน (authentication required)
- ✅ ดาวน์โหลดไฟล์ Excel พร้อมใช้งาน
- ✅ มีข้อมูลตัวอย่าง 2 แถว

---

### 3. **Import Excel Endpoint**
เพิ่ม API endpoint สำหรับ import ข้อมูลจาก Excel

**Endpoint:** `POST /api/master-data/customers/import`

**ไฟล์:** `backend_python/app/api/master_data.py` (บรรทัด 271-375)

**Features:**
- ✅ รองรับไฟล์ .xlsx และ .xls
- ✅ ตรวจสอบคอลัมน์ครบถ้วน (14 คอลัมน์)
- ✅ Validate รหัสลูกค้า (ต้องไม่มีช่องว่าง, ใช้ได้เฉพาะ A-Z, 0-9, -, _)
- ✅ ตรวจสอบรหัสลูกค้าซ้ำ
- ✅ รองรับข้อมูล optional fields (สามารถเว้นว่างได้)
- ✅ แสดงผลลัพธ์: จำนวนสำเร็จ, ล้มเหลว, รายละเอียดข้อผิดพลาด
- ✅ Skip แถวที่ว่าง
- ✅ แสดงข้อผิดพลาด 10 รายการแรก

**Response Format:**
```json
{
  "success": true,
  "message": "Import สำเร็จ 2 รายการ, ล้มเหลว 0 รายการ",
  "successCount": 2,
  "errorCount": 0,
  "errors": []
}
```

---

### 4. **Frontend Integration**
Frontend มีการ integrate อยู่แล้ว

**Component:** `frontend/src/components/modals/ExcelImportModal.jsx`

**Functions ที่มี:**
- `handleDownloadTemplate()` - ดาวน์โหลด template
- `handleUpload()` - อัปโหลดและ import Excel

**การใช้งาน:**
1. เปิด Customer List page
2. กดปุ่ม "Import Excel" (สีเขียว)
3. Modal จะแสดง 3 steps:
   - **Step 1:** ดาวน์โหลด Template (มีปุ่ม "ดาวน์โหลด Template")
   - **Step 2:** กรอกข้อมูลในไฟล์ Excel
   - **Step 3:** อัปโหลดไฟล์กลับเข้าระบบ

---

## 📦 Dependencies ที่เพิ่ม

```bash
pip install openpyxl  # สำหรับอ่าน/เขียนไฟล์ Excel
pip install requests  # สำหรับทดสอบ API
```

---

## 🧪 วิธีทดสอบ

### วิธีที่ 1: ใช้ Frontend (แนะนำ)
1. เปิด Backend: `cd backend_python && uvicorn app.main:app --reload --port 8000`
2. เปิด Frontend: `cd frontend && npm run dev`
3. Login เข้าระบบ
4. ไปที่หน้า "ข้อมูลลูกค้า"
5. กดปุ่ม "Import Excel"
6. กดปุ่ม "ดาวน์โหลด Template"
7. เปิดไฟล์ Excel และแก้ไขข้อมูล
8. กลับมาที่ Modal และกดปุ่ม "เลือกไฟล์" → เลือกไฟล์ที่แก้ไข
9. กดปุ่ม "อัปโหลดและ Import"

### วิธีที่ 2: ใช้ Swagger UI
1. เปิด Backend: `cd backend_python && uvicorn app.main:app --reload --port 8000`
2. เปิดเบราว์เซอร์: http://localhost:8000/docs
3. Login ผ่าน `/api/auth/login` (username: admin, password: admin123)
4. Copy token
5. กด "Authorize" ที่มุมขวาบน → ใส่ token
6. ทดสอบ `GET /api/master-data/customers/template` → Download template
7. ทดสอบ `POST /api/master-data/customers/import` → Upload ไฟล์

### วิธีที่ 3: ใช้ Python Script
```bash
cd backend_python
python test_template_feature.py
```

---

## 🔍 การตรวจสอบข้อมูลที่ Import

หลังจาก import แล้ว ตรวจสอบว่าข้อมูลถูกบันทึกครบถ้วน:

```python
# ใน Python console หรือ script
import asyncio
from app.database import get_db_context
from app.models.customer import Customer
from sqlalchemy import select

async def check_imported_customers():
    async with get_db_context() as db:
        result = await db.execute(select(Customer).order_by(Customer.id))
        customers = result.scalars().all()
        
        for c in customers:
            print(f"รหัส: {c.code}")
            print(f"  ประเภท: {c.businessType}")
            print(f"  ชื่อ: {c.name}")
            print(f"  ที่อยู่: {c.address}, {c.subDistrict}, {c.district}, {c.province} {c.postalCode}")
            print(f"  ติดต่อ: {c.contactPerson} ({c.phone})")
            print()

asyncio.run(check_imported_customers())
```

---

## ⚠️ ข้อควรระวัง

1. **รหัสลูกค้า (Code):**
   - ห้ามมีช่องว่าง
   - ใช้ได้เฉพาะ: A-Z, a-z, 0-9, -, _
   - ต้องไม่ซ้ำกับที่มีอยู่ในระบบ

2. **ประเภทธุรกิจ:**
   - แนะนำให้ใช้ค่าตามที่กำหนด: กิจการเจ้าของคนเดียว, ห้างหุ้นส่วน, บริษัทจำกัด, รัฐวิสาหกิจ
   - สามารถเว้นว่างได้

3. **ไฟล์ Excel:**
   - ต้องเป็นนามสกุล .xlsx หรือ .xls เท่านั้น
   - ห้ามลบหรือเปลี่ยนชื่อคอลัมน์
   - แถวแรกเป็น header (ห้ามลบ)

4. **ขนาดไฟล์:**
   - แนะนำไม่เกิน 1000 แถว ต่อครั้ง
   - ถ้ามีข้อมูลมาก ควรแบ่งเป็นหลายไฟล์

---

## 📝 สรุปไฟล์ที่เกี่ยวข้อง

### Backend Files:
- ✅ `backend_python/app/api/master_data.py` - เพิ่ม 2 endpoints
- ✅ `backend_python/templates/customer_template.xlsx` - Template file (อัปเดตแล้ว)
- ✅ `backend_python/update_template.py` - Script สร้าง template
- ✅ `backend_python/test_template_feature.py` - Script ทดสอบ

### Frontend Files:
- ✅ `frontend/src/components/modals/ExcelImportModal.jsx` - มีอยู่แล้ว, ใช้งานได้

### Dependencies:
- ✅ openpyxl - ติดตั้งแล้ว
- ✅ requests - ติดตั้งแล้ว
- ✅ pandas - มีอยู่แล้ว

---

## ✨ สิ่งที่ดีขึ้นจากเดิม

**เดิม (Old Template):**
- 8 คอลัมน์
- ที่อยู่เป็นข้อความยาวๆ เดียว
- ไม่มีประเภทธุรกิจ
- ไม่มีข้อมูลผู้ติดต่อรอง
- ไม่มีเงื่อนไขการชำระเงิน

**ใหม่ (New Template):**
- 14 คอลัมน์
- ที่อยู่แยกเป็น 5 ส่วน (บ้านเลขที่, แขวง/ตำบล, เขต/อำเภอ, จังหวัด, รหัสไปรษณีย์)
- มีประเภทธุรกิจ 4 แบบ
- มีช่องผู้ติดต่อรอง
- มีช่องเงื่อนไขการชำระเงิน
- รองรับโครงสร้างข้อมูลใหม่ครบถ้วน

---

## 🎯 Next Steps

1. ✅ Backend endpoints พร้อมใช้งาน
2. ✅ Frontend integration พร้อมใช้งาน
3. ✅ Template file อัปเดตแล้ว
4. ⏳ รอทดสอบกับ Backend Server ที่รันจริง
5. ⏳ อาจเพิ่ม validation บน Frontend ก่อนอัปโหลด
6. ⏳ อาจเพิ่มฟีเจอร์ Export ข้อมูลลูกค้าปัจจุบันเป็น Excel

---

**สร้างเมื่อ:** 4 ธันวาคม 2025  
**Status:** ✅ พร้อมใช้งาน
